from app.main import app

# Vercel needs a variable named 'app' to be the entry point
# We already have it in app.main, so we just import it.
# This file serves as the adapter/entry point for the Serverless Function.
